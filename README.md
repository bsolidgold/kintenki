# KinTenki

The official KinTenki Studios landing page.

## Local development

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Verification

```bash
npm test -- --run
npm run lint
npm run build
```

The `main` branch deploys automatically to the `kintenki` project on Vercel.
